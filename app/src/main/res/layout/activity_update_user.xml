<?xml version="1.0" encoding="utf-8"?>
<android.support.percent.PercentRelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/edit_text"
        fontPath="@string/medium"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="?attr/actionBarSize"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:gravity="center"
        android:imeOptions="actionSend"
        android:inputType="textPersonName|textNoSuggestions|text"
        android:lines="1"
        android:padding="5dp"
        android:text="@string/type_username_here"
        android:textColor="@color/colorTheme"
        android:textSize="30sp"
        app:layout_marginTopPercent="20%"/>

    <TextView
        android:id="@+id/text_title"
        fontPath="@string/light"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/edit_text"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:text="@string/setup_your_username_here"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        app:layout_marginLeftPercent="7%"
        app:layout_marginRightPercent="7%"/>

    <Button
        android:id="@+id/button_save"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:layout_below="@+id/text_title"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:alpha="0"
        android:background="@drawable/black_oval"
        android:gravity="center"
        android:minWidth="120dp"
        android:paddingBottom="2dp"
        android:text="@string/save"
        android:textSize="26sp"
        android:translationY="25dp"/>

    <ProgressBar
        android:id="@+id/progress"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_below="@+id/text_title"
        android:layout_centerHorizontal="true"
        android:indeterminate="true"
        android:translationY="-25dp"
        android:layout_marginTop="15dp"
        android:alpha="0"/>

    <android.support.v7.widget.CardView
        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        card_view:cardCornerRadius="0dp"
        card_view:cardElevation="4dp">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:minHeight="?attr/actionBarSize">

        </android.support.v7.widget.Toolbar>s
    </android.support.v7.widget.CardView>

</android.support.percent.PercentRelativeLayout>